var React = require('react');
var ReactDOM = require('react-dom');
var Dashboard = require('./components/Dashboard.jsx');

ReactDOM.render(<Dashboard />, document.getElementById('weatherDashboard'));
